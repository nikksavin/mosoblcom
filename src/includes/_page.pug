doctype html
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")

        title= title || 'Page name'

        // Styles::Start
        include _loadCss
        // Styles::End

        // Yandex Map
        if yandexMap == true
            script(src="https://api-maps.yandex.ru/2.1/?lang=ru_RU")

    body(class= bodyClass ? bodyClass : '')

        #page

            // Header::Start
            block header
                include _header
            // Header::End

            // Main::Start
            main#main
                block main
            // Main::End

            // Footer::Start
            block footer
                include _footer
            // Footer::End

        .popup-form#popup-form
            .popup-form__header
                .popup-form__title Есть проект, который нужно реализовать?
                .popup-form__subtitle Просто позвоните по телефону <br><a href="tel:+74952920202">+7 495 292-02-02</a><br> или заполните форму:
            include ../components/_form

        // Scripts::Start
        include _loadScripts
        // Scripts::End